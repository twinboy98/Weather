# Privacy

Browser geolocation is ephemeral unless the user explicitly saves a location. Provider requests round coordinates to the resolution required by each provider; MET Norway is capped at four decimal places. Exact personal addresses and private location files are ignored by Git. Public exports use only opted-in benchmark locations and must pseudonymize location IDs.

